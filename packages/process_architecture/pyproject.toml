[project]
name = "eaos-process-architecture"
version = "0.1.0"
description = "EAOS process_architecture Domain Package"
readme = "README.md"
requires-python = ">=3.14"

dependencies = []

[build-system]
requires = ["hatchling>=1.28"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]