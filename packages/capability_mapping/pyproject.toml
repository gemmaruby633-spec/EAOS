[project]
name = "eaos-capability-mapping"
version = "0.1.0"
description = "EAOS capability_mapping Domain Package"
readme = "README.md"
requires-python = ">=3.14"

dependencies = []

[build-system]
requires = ["hatchling>=1.28"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]